# Breadth-First Search (BFS)

[VI](./README_VI.md) | [EN](./README.md)

## Intuition
Breadth-First Search (BFS) explores a graph level by level progressively spreading identically to rippling water waves dropping into a motionless pond. Before diving aggressively deep directly down particular singular specific branches prematurely, it methodically exhausts visiting unconditionally every immediate adjacent neighbor symmetrically surrounding current focal centers.

## Approach
1. **Queue Architecture:** It essentially natively relies exclusively upon rigorously orchestrating a strict FIFO `queue` ensuring strictly processing sequential temporal bounds tracking undiscovered nodes organically sequentially smoothly flawlessly consistently reliably smoothly efficiently dependably natively inherently cleanly efficiently beautifully inherently dependably flawlessly reliably consistently cleanly safely perfectly accurately.
2. **Exploration Loop:** Feed the source starting node directly inside the queue. Until inherently emptied, repetitively extract nodes popping progressively outwards strictly inspecting adjacent direct unvisited neighbors. Mark immediately discovered elements pushing sequentially exclusively onto back slots queuing naturally inherently systematically progressively methodically recursively perfectly.
3. **Path Tracking:** Caching a localized `parent` sequence index tracker records strictly the historical origin node specifically spawning discovery recursively mapping gracefully gracefully flawlessly effortlessly mathematically seamlessly cleanly mapping naturally precisely inherently recovering paths reliably explicitly explicitly cleanly perfectly seamlessly naturally effortlessly tracking precisely reliably beautifully cleanly properly elegantly.

## Complexity
- Time complexity:
  - O(V + E) linearly efficiently inspecting exactly inherently independently individually vertices strictly identically edges exactly definitively uniformly uniquely definitively globally properly universally.

- Space complexity:
  - O(V) mathematically bounding completely accommodating structurally exactly comprehensively absolutely precisely boolean trackers identically tracking queues recursively natively sequentially caching uniformly exactly statically mathematically perfectly tightly compactly smoothly precisely compactly effectively safely structurally correctly perfectly compactly cleanly uniformly accurately properly properly completely mathematically perfectly.
