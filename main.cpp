#include "bits/stdc++.h"

using namespace std;

#define nhan43                        \
    ios_base::sync_with_stdio(false); \
    cin.tie(nullptr);                 \
    cout.tie(nullptr);

void fastIO()
{
    nhan43;
    if (fopen("main.inp", "r"))
    {
        freopen("main.inp", "r", stdin);
        freopen("main.out", "w", stdout);
    }
}

int tc;

int main()
{
    fastIO();

    int tc = 1;
    // cin >> tc;

    while (tc--)
    {
        
    }

    return 0;
}